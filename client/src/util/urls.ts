const API_GLOBAL_PREFIX = '/api';

const urls = {
  api: {
    signup: `${API_GLOBAL_PREFIX}/user/signup`,
    login: `${API_GLOBAL_PREFIX}/user/login`
  },
  login: '/login',
  signup: '/signup'
};

export default urls;
